This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
backend/
  data_generator.py
  main.py
  models.py
frontend/
  public/
    file.svg
    globe.svg
    next.svg
    vercel.svg
    window.svg
  src/
    app/
      favicon.ico
      globals.css
      layout.tsx
      page.tsx
  .gitignore
  AGENTS.md
  CLAUDE.md
  eslint.config.mjs
  next.config.ts
  package.json
  postcss.config.mjs
  README.md
  tsconfig.json
commands.txt
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="frontend/public/file.svg">
<svg fill="none" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg"><path d="M14.5 13.5V5.41a1 1 0 0 0-.3-.7L9.8.29A1 1 0 0 0 9.08 0H1.5v13.5A2.5 2.5 0 0 0 4 16h8a2.5 2.5 0 0 0 2.5-2.5m-1.5 0v-7H8v-5H3v12a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1M9.5 5V2.12L12.38 5zM5.13 5h-.62v1.25h2.12V5zm-.62 3h7.12v1.25H4.5zm.62 3h-.62v1.25h7.12V11z" clip-rule="evenodd" fill="#666" fill-rule="evenodd"/></svg>
</file>

<file path="frontend/public/globe.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><g clip-path="url(#a)"><path fill-rule="evenodd" clip-rule="evenodd" d="M10.27 14.1a6.5 6.5 0 0 0 3.67-3.45q-1.24.21-2.7.34-.31 1.83-.97 3.1M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16m.48-1.52a7 7 0 0 1-.96 0H7.5a4 4 0 0 1-.84-1.32q-.38-.89-.63-2.08a40 40 0 0 0 3.92 0q-.25 1.2-.63 2.08a4 4 0 0 1-.84 1.31zm2.94-4.76q1.66-.15 2.95-.43a7 7 0 0 0 0-2.58q-1.3-.27-2.95-.43a18 18 0 0 1 0 3.44m-1.27-3.54a17 17 0 0 1 0 3.64 39 39 0 0 1-4.3 0 17 17 0 0 1 0-3.64 39 39 0 0 1 4.3 0m1.1-1.17q1.45.13 2.69.34a6.5 6.5 0 0 0-3.67-3.44q.65 1.26.98 3.1M8.48 1.5l.01.02q.41.37.84 1.31.38.89.63 2.08a40 40 0 0 0-3.92 0q.25-1.2.63-2.08a4 4 0 0 1 .85-1.32 7 7 0 0 1 .96 0m-2.75.4a6.5 6.5 0 0 0-3.67 3.44 29 29 0 0 1 2.7-.34q.31-1.83.97-3.1M4.58 6.28q-1.66.16-2.95.43a7 7 0 0 0 0 2.58q1.3.27 2.95.43a18 18 0 0 1 0-3.44m.17 4.71q-1.45-.12-2.69-.34a6.5 6.5 0 0 0 3.67 3.44q-.65-1.27-.98-3.1" fill="#666"/></g><defs><clipPath id="a"><path fill="#fff" d="M0 0h16v16H0z"/></clipPath></defs></svg>
</file>

<file path="frontend/public/next.svg">
<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 394 80"><path fill="#000" d="M262 0h68.5v12.7h-27.2v66.6h-13.6V12.7H262V0ZM149 0v12.7H94v20.4h44.3v12.6H94v21h55v12.6H80.5V0h68.7zm34.3 0h-17.8l63.8 79.4h17.9l-32-39.7 32-39.6h-17.9l-23 28.6-23-28.6zm18.3 56.7-9-11-27.1 33.7h17.8l18.3-22.7z"/><path fill="#000" d="M81 79.3 17 0H0v79.3h13.6V17l50.2 62.3H81Zm252.6-.4c-1 0-1.8-.4-2.5-1s-1.1-1.6-1.1-2.6.3-1.8 1-2.5 1.6-1 2.6-1 1.8.3 2.5 1a3.4 3.4 0 0 1 .6 4.3 3.7 3.7 0 0 1-3 1.8zm23.2-33.5h6v23.3c0 2.1-.4 4-1.3 5.5a9.1 9.1 0 0 1-3.8 3.5c-1.6.8-3.5 1.3-5.7 1.3-2 0-3.7-.4-5.3-1s-2.8-1.8-3.7-3.2c-.9-1.3-1.4-3-1.4-5h6c.1.8.3 1.6.7 2.2s1 1.2 1.6 1.5c.7.4 1.5.5 2.4.5 1 0 1.8-.2 2.4-.6a4 4 0 0 0 1.6-1.8c.3-.8.5-1.8.5-3V45.5zm30.9 9.1a4.4 4.4 0 0 0-2-3.3 7.5 7.5 0 0 0-4.3-1.1c-1.3 0-2.4.2-3.3.5-.9.4-1.6 1-2 1.6a3.5 3.5 0 0 0-.3 4c.3.5.7.9 1.3 1.2l1.8 1 2 .5 3.2.8c1.3.3 2.5.7 3.7 1.2a13 13 0 0 1 3.2 1.8 8.1 8.1 0 0 1 3 6.5c0 2-.5 3.7-1.5 5.1a10 10 0 0 1-4.4 3.5c-1.8.8-4.1 1.2-6.8 1.2-2.6 0-4.9-.4-6.8-1.2-2-.8-3.4-2-4.5-3.5a10 10 0 0 1-1.7-5.6h6a5 5 0 0 0 3.5 4.6c1 .4 2.2.6 3.4.6 1.3 0 2.5-.2 3.5-.6 1-.4 1.8-1 2.4-1.7a4 4 0 0 0 .8-2.4c0-.9-.2-1.6-.7-2.2a11 11 0 0 0-2.1-1.4l-3.2-1-3.8-1c-2.8-.7-5-1.7-6.6-3.2a7.2 7.2 0 0 1-2.4-5.7 8 8 0 0 1 1.7-5 10 10 0 0 1 4.3-3.5c2-.8 4-1.2 6.4-1.2 2.3 0 4.4.4 6.2 1.2 1.8.8 3.2 2 4.3 3.4 1 1.4 1.5 3 1.5 5h-5.8z"/></svg>
</file>

<file path="frontend/public/vercel.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1155 1000"><path d="m577.3 0 577.4 1000H0z" fill="#fff"/></svg>
</file>

<file path="frontend/public/window.svg">
<svg fill="none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill-rule="evenodd" clip-rule="evenodd" d="M1.5 2.5h13v10a1 1 0 0 1-1 1h-11a1 1 0 0 1-1-1zM0 1h16v11.5a2.5 2.5 0 0 1-2.5 2.5h-11A2.5 2.5 0 0 1 0 12.5zm3.75 4.5a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5M7 4.75a.75.75 0 1 1-1.5 0 .75.75 0 0 1 1.5 0m1.75.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5" fill="#666"/></svg>
</file>

<file path="frontend/src/app/globals.css">
@import "tailwindcss";

:root {
  --background: #030712; /* Mandatory Obsidian Black [cite: 11] */
  --foreground: #ffffff;
}

body {
  background-color: #030712;
  color: #ffffff;
  font-family: 'Inter', sans-serif; /* [cite: 16] */
}
</file>

<file path="frontend/src/app/layout.tsx">
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
</file>

<file path="frontend/.gitignore">
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
</file>

<file path="frontend/AGENTS.md">
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->
</file>

<file path="frontend/CLAUDE.md">
@AGENTS.md
</file>

<file path="frontend/eslint.config.mjs">
import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
</file>

<file path="frontend/next.config.ts">
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
</file>

<file path="frontend/postcss.config.mjs">
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
</file>

<file path="frontend/README.md">
This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.

This project uses [`next/font`](https://nextjs.org/docs/app/building-your-application/optimizing/fonts) to automatically optimize and load [Geist](https://vercel.com/font), a new font family for Vercel.

## Learn More

To learn more about Next.js, take a look at the following resources:

- [Next.js Documentation](https://nextjs.org/docs) - learn about Next.js features and API.
- [Learn Next.js](https://nextjs.org/learn) - an interactive Next.js tutorial.

You can check out [the Next.js GitHub repository](https://github.com/vercel/next.js) - your feedback and contributions are welcome!

## Deploy on Vercel

The easiest way to deploy your Next.js app is to use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme) from the creators of Next.js.

Check out our [Next.js deployment documentation](https://nextjs.org/docs/app/building-your-application/deploying) for more details.
</file>

<file path="frontend/tsconfig.json">
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}
</file>

<file path="backend/main.py">
import pandas as pd
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from data_generator import get_mock_data, find_recurring_spends

app = FastAPI(title="Real Rails: Finance Graph")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["http://localhost:3000"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

@app.get("/intelligence/rail-data")
def get_rail_intelligence():
    account, transactions = get_mock_data()
    df = pd.DataFrame([t.model_dump() for t in transactions])
    
    # Separate Income and Outflow using Pandas
    is_income = df['category'].apply(lambda c: 'Income' in c)
    total_income = df[is_income]['amount'].sum()
    total_spent = df[~is_income]['amount'].sum()
    
    recurring = find_recurring_spends(transactions)
    
    # --- NEW: DETERMINISTIC AI MODEL ---
    # 1. Calculate how much of the 90-day spend was purely recurring bills
    recurring_90d_total = sum(item["total_spent_90d"] for item in recurring)
    
    # 2. Isolate the "Noise" (Food, Uber, etc.)
    noise_90d_total = total_spent - recurring_90d_total
    
    # 3. Calculate the true daily burn rate for the random stuff
    daily_noise_rate = noise_90d_total / 90
    
    # Overall Runway
    daily_burn_rate = total_spent / 90
    runway_days = int(account.balances.current / daily_burn_rate) if daily_burn_rate > 0 else 999
    
    return {
        "main_stage": {
            "transactions": transactions,
        },
        "sidebar": {
            "metrics": {
                "total_spent": float(total_spent),
                "total_income": float(total_income),
                "runway_days": runway_days,
                "daily_noise_rate": float(daily_noise_rate) # Passing the new AI calculation
            },
            "why_it_matters": "Financial rails are the infrastructure of personal stability.",
            "governance": "Individual control over institutional payment cycles.",
            "insights": recurring
        }
    }
</file>

<file path="frontend/package.json">
{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.2",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.2",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
</file>

<file path="backend/models.py">
from pydantic import BaseModel, Field
from typing import Optional, List
from datetime import date  # <--- This is the missing piece!

# 1. Balance Schema
class Balance(BaseModel):
    available: Optional[float] = None
    current: float
    limit: Optional[float] = None
    currency: str = "INR"  # <-- Changed from USD

# 2. Account Schema
class Account(BaseModel):
    account_id: str
    name: str
    type: str
    subtype: str
    balances: Balance

# 3. Transaction Schema
class Transaction(BaseModel):
    transaction_id: str
    account_id: str
    amount: float
    date: date  # Pydantic now knows this refers to the import above
    name: str
    category: List[str]
    pending: bool = False
</file>

<file path="commands.txt">
1. To run /backend : uvicorn main:app --reload
2. To run /frontend : npm run dev

Frontend localhost face : http://localhost:3000
backend localhost api : http://127.0.0.1:8000/docs#/
backend intelligent json : http://127.0.0.1:8000/intelligence/rail-data

repomix generator : npx repomix
activate environment : source ../venv/bin/activate
</file>

<file path="backend/data_generator.py">
from datetime import date, timedelta
import random
from models import Account, Balance, Transaction

def get_mock_data():
    account = Account(
        account_id="acc_001",
        name="Main Checking",
        type="depository",
        subtype="checking",
        balances=Balance(available=145000.0, current=150000.0)
    )

    transactions = []
    start_date = date.today() - timedelta(days=90)

    # --- NEW: PHASE 2 INCOME GENERATOR ---
    for i in range(3): # 3 months of salary history
        tx_date = start_date + timedelta(days=(i * 30) + 1)
        transactions.append(Transaction(
            transaction_id=f"inc_{i}",
            account_id="acc_001",
            amount=85000.00, # Realistic INR Monthly Salary
            date=tx_date,
            name="TechCorp Salary",
            category=["Income", "Salary"] # Tagged for the algorithm
        ))

    # Add Recurring "Ghost" Subscriptions
    for i in range(3):
        tx_date = start_date + timedelta(days=i*30)
        transactions.append(Transaction(
            transaction_id=f"ghost_{i}",
            account_id="acc_001",
            amount=499.00,
            date=tx_date,
            name="Netflix Subscription",
            category=["Service", "Entertainment"]
        ))
        
        transactions.append(Transaction(
            transaction_id=f"gym_{i}",
            account_id="acc_001",
            amount=1999.00,
            date=tx_date + timedelta(days=5),
            name="Gold Fitness",
            category=["Health", "Fitness"]
        ))

    # Add Random Daily Spends
    for i in range(60):
        random_date = start_date + timedelta(days=random.randint(0, 90))
        merchant_name = random.choice(["Uber", "Starbucks", "Amazon", "Zomato", "Swiggy", "Blinkit"])
        
        if merchant_name in ["Uber", "Zomato", "Swiggy"]:
            amt = round(random.uniform(150.0, 600.0), 2)
        elif merchant_name == "Starbucks":
            amt = round(random.uniform(300.0, 900.0), 2)
        elif merchant_name == "Blinkit":
            amt = round(random.uniform(400.0, 1500.0), 2)
        else:
            amt = round(random.uniform(800.0, 4500.0), 2)

        transactions.append(Transaction(
            transaction_id=f"rand_{i}",
            account_id="acc_001",
            amount=amt,
            date=random_date,
            name=merchant_name,
            category=["Food" if i%2==0 else "Travel"]
        ))

    return account, transactions

def find_recurring_spends(transactions):
    analysis = {}
    # Ignore Income when looking for ghost subscriptions
    expenses = [tx for tx in transactions if "Income" not in tx.category]
    
    for tx in expenses:
        if tx.name not in analysis:
            analysis[tx.name] = []
        analysis[tx.name].append(tx)

    recurring = []
    for name, tx_list in analysis.items():
        if len(tx_list) >= 3:
            recurring.append({
                "name": name,
                "frequency": "Monthly",
                "amount": tx_list[0].amount,
                "last_date": max(t.date for t in tx_list),
                "total_spent_90d": sum(t.amount for t in tx_list)
            })
    return recurring
</file>

<file path="frontend/src/app/page.tsx">
'use client';
import { useEffect, useState } from 'react';
import { 
  AreaChart, Area, XAxis, YAxis, CartesianGrid, 
  Tooltip, ResponsiveContainer, ReferenceLine
} from 'recharts';

export default function RealRailsDashboard() {
  const [data, setData] = useState<any>(null);
  const [searchTerm, setSearchTerm] = useState('');
  const [timeRange, setTimeRange] = useState(90);
  const [selectedDate, setSelectedDate] = useState<string | null>(null);
  const [isMounted, setIsMounted] = useState(false); 
  const [activeView, setActiveView] = useState<'history' | 'forecast'>('history');

  useEffect(() => {
    setIsMounted(true);
    fetch('http://127.0.0.1:8000/intelligence/rail-data')
      .then(res => res.json())
      .then(json => setData(json))
      .catch(err => console.error("Connection Error:", err));
  }, []);

  if (!data) return (
    <div className="h-screen bg-[#030712] flex items-center justify-center text-[#38BDF8] font-mono animate-pulse">
      CALCULATING_DETERMINISTIC_RUNWAY...
    </div>
  );

  const filteredTransactions = data.main_stage.transactions.filter((tx: any) => 
    tx.name.toLowerCase().includes(searchTerm.toLowerCase()) || 
    tx.category.some((cat: string) => cat.toLowerCase().includes(searchTerm.toLowerCase()))
  );

  // --- HISTORICAL DATA ---
  const sortedChartData = data.main_stage.transactions.slice().sort((a:any, b:any) => new Date(a.date).getTime() - new Date(b.date).getTime());
  const latestDateObj = sortedChartData.length > 0 ? new Date(sortedChartData[sortedChartData.length - 1].date) : new Date();
  const cutoffTime = latestDateObj.getTime() - (timeRange * 24 * 60 * 60 * 1000);

  const dailyTotals: Record<string, number> = {};
  for (let i = timeRange - 1; i >= 0; i--) {
    const d = new Date(latestDateObj);
    d.setDate(d.getDate() - i);
    const dateString = d.toISOString().split('T')[0];
    dailyTotals[dateString] = 0;
  }

  const dynamicTransactions = sortedChartData.filter((tx: any) => new Date(tx.date).getTime() >= cutoffTime);
  const expenseChartData = dynamicTransactions.filter((tx: any) => !tx.category.includes('Income'));
  
  expenseChartData.forEach((tx: any) => {
    if (dailyTotals[tx.date] !== undefined) {
      dailyTotals[tx.date] += tx.amount;
    } else {
      dailyTotals[tx.date] = tx.amount;
    }
  });

  const finalChartData = Object.keys(dailyTotals).map(date => ({
    date,
    amount: dailyTotals[date]
  })).sort((a, b) => new Date(a.date).getTime() - new Date(b.date).getTime());

  // --- MATH & METRICS ---
  const dynamicIncome = dynamicTransactions.filter((tx: any) => tx.category.includes('Income')).reduce((sum: number, tx: any) => sum + tx.amount, 0);
  const dynamicOutflow = expenseChartData.reduce((sum: number, tx: any) => sum + tx.amount, 0);

  const mockCurrentBalance = 150000; 
  const dailyBurnRate = dynamicOutflow / timeRange;
  const dynamicRunway = dailyBurnRate > 0 ? Math.round(mockCurrentBalance / dailyBurnRate) : 999;


  // --- UPDATED: DETERMINISTIC FORECAST SIMULATOR ---
  const forecastData = [];
  let currentSimBalance = mockCurrentBalance;
  let dayCount = 0;

  // We now store the WHOLE bill object, not just the math
  const recurringBillsByDay: Record<number, any[]> = {};
  data.sidebar.insights.forEach((sub: any) => {
      const billDay = new Date(sub.last_date).getDate();
      if (!recurringBillsByDay[billDay]) recurringBillsByDay[billDay] = [];
      recurringBillsByDay[billDay].push(sub);
  });

  const dailyNoiseRate = data.sidebar.metrics.daily_noise_rate || dailyBurnRate;
  const currentDate = new Date();

  while (currentSimBalance > 0 && dayCount <= 365) {
      const simDate = new Date(currentDate);
      simDate.setDate(simDate.getDate() + dayCount);
      const simDayOfMonth = simDate.getDate();

      currentSimBalance -= dailyNoiseRate;
      let billsToday = 0;
      let billsHit: any[] = []; // Stores the exact bills that hit today

      if (recurringBillsByDay[simDayOfMonth]) {
          recurringBillsByDay[simDayOfMonth].forEach(bill => {
              billsToday += bill.amount;
              billsHit.push(bill); // Pack the name and amount!
          });
          currentSimBalance -= billsToday;
      }

      forecastData.push({
          date: simDate.toISOString().split('T')[0],
          balance: Math.max(0, currentSimBalance),
          isBillDay: billsToday > 0,
          billsHit: billsHit, // Sent directly to the chart
          totalDrainedToday: dailyNoiseRate + billsToday
      });

      dayCount++;
  }

  // DRILLDOWN DATA SORTER
  const historyDrilldown = selectedDate && activeView === 'history' ? data.main_stage.transactions.filter((tx: any) => tx.date === selectedDate) : [];
  const forecastDrilldown = selectedDate && activeView === 'forecast' ? forecastData.find(d => d.date === selectedDate) : null;

  return (
    <main className="flex h-screen bg-[#030712] overflow-hidden text-white font-sans selection:bg-[#38BDF8] selection:text-[#030712]">
      
      {/* UPDATED: UNIFIED DAILY DRILLDOWN MODAL */}
      {selectedDate && (
        <div className="absolute inset-0 z-50 flex items-center justify-center bg-[#030712]/80 backdrop-blur-sm p-4 animate-in fade-in duration-200">
          <div className={`bg-[#0B1117] border ${activeView === 'history' ? 'border-[#38BDF8]/50 shadow-[0_0_40px_rgba(56,189,248,0.15)]' : 'border-[#818CF8]/50 shadow-[0_0_40px_rgba(129,140,248,0.15)]'} rounded-2xl p-8 max-w-md w-full relative`}>
            <button onClick={() => setSelectedDate(null)} className={`absolute top-5 right-5 text-gray-500 transition-colors ${activeView === 'history' ? 'hover:text-[#38BDF8]' : 'hover:text-[#818CF8]'}`}>✕</button>
            <p className={`${activeView === 'history' ? 'text-[#38BDF8]' : 'text-[#818CF8]'} text-[10px] font-bold tracking-[0.2em] uppercase mb-1`}>
              {activeView === 'history' ? 'Daily Drilldown' : 'Forecasted Drain'}
            </p>
            <h3 className="text-2xl font-black text-white mb-6">{new Date(selectedDate).toLocaleDateString('en-US', { weekday: 'long', month: 'long', day: 'numeric' })}</h3>
            
            <div className="space-y-3 max-h-64 overflow-y-auto pr-2 custom-scrollbar">
              
              {/* HISTORY VIEW UI */}
              {activeView === 'history' && historyDrilldown.length > 0 ? (
                historyDrilldown.map((tx: any) => (
                  <div key={tx.transaction_id} className="flex justify-between items-center p-4 bg-[#030712] border border-[#1F2937] rounded-lg hover:border-[#38BDF8]/30 transition-colors">
                    <div>
                      <p className="font-bold text-gray-300">{tx.name}</p>
                      <p className="text-[9px] text-gray-500 uppercase tracking-widest mt-0.5">{tx.category[0]}</p>
                    </div>
                    <p className={`font-mono font-black text-lg ${tx.category.includes('Income') ? 'text-green-400' : 'text-[#38BDF8]'}`}>
                      {tx.category.includes('Income') ? '+' : ''}₹{tx.amount.toFixed(2)}
                    </p>
                  </div>
                ))
              ) : activeView === 'history' && (
                <p className="text-gray-500 italic text-sm text-center py-4">No activity recorded on this day.</p>
              )}

              {/* FORECAST VIEW UI */}
              {activeView === 'forecast' && forecastDrilldown && (
                <>
                  <div className="flex justify-between items-center p-4 bg-[#030712] border border-[#1F2937] rounded-lg">
                    <div>
                      <p className="font-bold text-gray-300">Daily Average Spend</p>
                      <p className="text-[9px] text-gray-500 uppercase tracking-widest mt-0.5">Projected Noise</p>
                    </div>
                    <p className="font-mono font-black text-gray-400 text-lg">₹{dailyNoiseRate.toFixed(2)}</p>
                  </div>

                  {forecastDrilldown.isBillDay && forecastDrilldown.billsHit.map((bill: any, idx: number) => (
                    <div key={idx} className="flex justify-between items-center p-4 bg-[#030712] border border-[#ef4444]/50 rounded-lg shadow-inner">
                      <div>
                        <p className="font-bold text-white">{bill.name}</p>
                        <p className="text-[9px] text-[#ef4444] uppercase tracking-widest mt-0.5">Automated Deduction</p>
                      </div>
                      <p className="font-mono font-black text-[#ef4444] text-lg">₹{bill.amount.toFixed(2)}</p>
                    </div>
                  ))}
                </>
              )}
            </div>

            <div className="mt-6 pt-4 border-t border-[#1F2937] flex justify-between items-end">
              <p className="text-xs text-gray-500 uppercase tracking-widest font-bold">
                {activeView === 'history' ? 'Total Disbursed' : 'Total Projected Drain'}
              </p>
              <p className="text-2xl font-black text-white">
                ₹{activeView === 'history' 
                  ? (dailyTotals[selectedDate]?.toFixed(2) || "0.00") 
                  : (forecastDrilldown?.totalDrainedToday.toFixed(2) || "0.00")}
              </p>
            </div>

            {/* ONLY ON FORECAST: Show remaining balance after hits */}
            {activeView === 'forecast' && (
              <div className="mt-2 flex justify-between items-end">
                <p className="text-xs text-gray-500 uppercase tracking-widest font-bold">Ending Balance</p>
                <p className="text-lg font-black text-[#818CF8]">₹{forecastDrilldown?.balance.toLocaleString('en-IN', { maximumFractionDigits: 2 })}</p>
              </div>
            )}
          </div>
        </div>
      )}

      {/* MAIN STAGE (70%) */}
      <section className="w-[70%] p-10 border-r border-[#1F2937] flex flex-col relative overflow-y-auto">
        <header className="mb-10 shrink-0 flex justify-between items-end">
          <div>
            <div className="flex items-center gap-3 mb-2">
              <div className="w-2 h-2 rounded-full bg-[#38BDF8] shadow-[0_0_8px_#38BDF8]"></div>
              <p className="text-[#38BDF8] text-[10px] font-bold tracking-[0.3em] uppercase">System Live</p>
            </div>
            <h1 className="text-white font-black text-6xl tracking-tighter italic uppercase">Finance<span className="text-[#38BDF8]">_Rail</span></h1>
          </div>
          
          <div className="flex bg-[#0B1117] border border-[#1F2937] p-1 rounded-lg">
            <button onClick={() => setActiveView('history')} className={`px-6 py-2 text-xs font-bold uppercase tracking-widest rounded transition-all ${activeView === 'history' ? 'bg-[#38BDF8] text-[#030712] shadow-[0_0_10px_rgba(56,189,248,0.5)]' : 'text-gray-500 hover:text-white'}`}>Historical Flow</button>
            <button onClick={() => setActiveView('forecast')} className={`px-6 py-2 text-xs font-bold uppercase tracking-widest rounded transition-all ${activeView === 'forecast' ? 'bg-[#818CF8] text-[#030712] shadow-[0_0_10px_rgba(129,140,248,0.5)]' : 'text-gray-500 hover:text-white'}`}>Runway Forecast</button>
          </div>
        </header>

        {/* ANALYTICS CONTAINER */}
        <div className="h-[400px] shrink-0 bg-[#0B1117] border border-[#1F2937] rounded-xl p-8 relative shadow-2xl overflow-hidden group mb-8">
          
          {activeView === 'history' ? (
            <div className="absolute top-6 left-8 right-8 z-10 flex justify-between items-start pointer-events-none">
              <div>
                <p className="text-[#38BDF8] text-[10px] font-bold tracking-widest uppercase opacity-60">{timeRange}D_SPENDING_VELOCITY</p>
                <h3 className="text-2xl font-bold mt-1">Institutional Flow</h3>
                <p className="text-xs text-gray-500 mt-1 pointer-events-auto">Click any point to view daily drilldown</p>
              </div>
              <div className="flex gap-2 pointer-events-auto">
                {[7, 30, 60, 90].map(days => (
                  <button key={days} onClick={() => setTimeRange(days)} className={`px-3 py-1.5 text-[10px] font-bold uppercase tracking-widest rounded transition-all ${timeRange === days ? 'bg-[#38BDF8] text-[#030712] shadow-[0_0_10px_rgba(56,189,248,0.5)]' : 'bg-[#030712] text-gray-500 border border-[#1F2937] hover:border-[#38BDF8] hover:text-[#38BDF8]'}`}>
                    {days} Days
                  </button>
                ))}
              </div>
            </div>
          ) : (
            <div className="absolute top-6 left-8 right-8 z-10 flex justify-between items-start pointer-events-none">
              <div>
                <p className="text-[#818CF8] text-[10px] font-bold tracking-widest uppercase opacity-60">DETERMINISTIC_MODELING</p>
                <h3 className="text-2xl font-bold mt-1">Cash Runway Burn-Down</h3>
                <p className="text-xs text-gray-400 mt-1 italic pointer-events-auto">
                  Click any <span className="text-[#ef4444] font-bold">Red Dot</span> to view automated deduction details.
                </p>
              </div>
            </div>
          )}
          
          {isMounted && (
            <ResponsiveContainer width="100%" height="100%" className="cursor-pointer">
              {activeView === 'history' ? (
                <AreaChart data={finalChartData} margin={{ top: 70, right: 0, left: -20, bottom: 0 }} onClick={(e: any) => { if (e && e.activeLabel) { setSelectedDate(e.activeLabel); } }}>
                  <defs>
                    <linearGradient id="cyanGradient" x1="0" y1="0" x2="0" y2="1">
                      <stop offset="5%" stopColor="#38BDF8" stopOpacity={0.4}/>
                      <stop offset="95%" stopColor="#38BDF8" stopOpacity={0}/>
                    </linearGradient>
                  </defs>
                  <CartesianGrid strokeDasharray="3 3" stroke="#1F2937" vertical={false} />
                  <XAxis dataKey="date" stroke="#4B5563" fontSize={10} tickLine={false} axisLine={false} tickFormatter={(str) => new Date(str).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })} />
                  <YAxis stroke="#4B5563" fontSize={10} tickLine={false} axisLine={false} tickFormatter={(value) => `₹${value}`} />
                  <Tooltip contentStyle={{ backgroundColor: '#0B1117', border: '1px solid #1F2937', borderRadius: '8px' }} itemStyle={{ color: '#38BDF8', fontWeight: 'bold' }} cursor={{ stroke: '#38BDF8', strokeWidth: 1 }} formatter={(value: number) => [`₹${value.toFixed(2)}`, 'Daily Disbursement']} />
                  <Area type="monotone" dataKey="amount" stroke="#38BDF8" fillOpacity={1} fill="url(#cyanGradient)" strokeWidth={3} animationDuration={1000} activeDot={{ r: 6, fill: '#030712', stroke: '#38BDF8', strokeWidth: 2 }} />
                </AreaChart>
              ) : (
                <AreaChart data={forecastData} margin={{ top: 90, right: 20, left: -10, bottom: 0 }} onClick={(e: any) => { if (e && e.activeLabel) { setSelectedDate(e.activeLabel); } }}>
                  <defs>
                    <linearGradient id="indigoGradient" x1="0" y1="0" x2="0" y2="1">
                      <stop offset="5%" stopColor="#818CF8" stopOpacity={0.5}/>
                      <stop offset="95%" stopColor="#ef4444" stopOpacity={0.1}/>
                    </linearGradient>
                  </defs>
                  <CartesianGrid strokeDasharray="3 3" stroke="#1F2937" vertical={false} />
                  <XAxis dataKey="date" stroke="#4B5563" fontSize={10} tickLine={false} axisLine={false} tickFormatter={(str) => new Date(str).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })} />
                  <YAxis stroke="#4B5563" fontSize={10} tickLine={false} axisLine={false} tickFormatter={(value) => `₹${value.toLocaleString()}`} />
                  
                  <Tooltip 
                    contentStyle={{ backgroundColor: '#0B1117', border: '1px solid #1F2937', borderRadius: '8px' }} 
                    itemStyle={{ color: '#818CF8', fontWeight: 'bold' }} 
                    cursor={{ stroke: '#818CF8', strokeWidth: 1, strokeDasharray: "5 5" }} 
                    labelFormatter={(label) => new Date(label).toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' })}
                    formatter={(value: number, name: string, props: any) => [
                      `₹${value.toLocaleString()}`, 
                      props.payload.isBillDay ? 'Balance (Recurring Bill Hit!)' : 'Projected Balance'
                    ]} 
                  />
                  <ReferenceLine y={0} stroke="#ef4444" strokeDasharray="3 3" label={{ position: 'insideBottomRight', value: 'Zero Liquidity Line', fill: '#ef4444', fontSize: 10 }} />
                  
                  <Area 
                    type="monotone" 
                    dataKey="balance" 
                    stroke="#818CF8" 
                    fillOpacity={1} 
                    fill="url(#indigoGradient)" 
                    strokeWidth={3} 
                    animationDuration={1500} 
                    activeDot={{ r: 6, fill: '#030712', stroke: '#818CF8', strokeWidth: 2 }}
                    dot={(props: any) => props.payload.isBillDay ? <circle cx={props.cx} cy={props.cy} r={4} fill="#ef4444" stroke="#0B1117" strokeWidth={2} /> : null}
                  />
                </AreaChart>
              )}
            </ResponsiveContainer>
          )}
        </div>

        {/* PHASE 1 CATEGORIZATION TABLE */}
        <div className="bg-[#0B1117] border border-[#1F2937] rounded-xl p-8 shadow-2xl shrink-0">
          <div className="flex justify-between items-end mb-6">
            <div>
              <p className="text-[#38BDF8] text-[10px] font-bold tracking-widest uppercase opacity-60">Phase 1 Baseline</p>
              <h3 className="text-2xl font-bold mt-1">Transaction Ledger</h3>
            </div>
            <input type="text" placeholder="Search merchants or categories..." className="bg-[#030712] border border-[#1F2937] text-white px-4 py-2 rounded-lg focus:outline-none focus:border-[#38BDF8] transition-colors w-72 text-sm placeholder-gray-600" value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} />
          </div>

          <div className="overflow-x-auto">
            <table className="w-full text-left text-sm">
              <thead className="text-[#818CF8] text-[10px] uppercase tracking-widest border-b border-[#1F2937]">
                <tr>
                  <th className="pb-3 font-bold">Date</th>
                  <th className="pb-3 font-bold">Merchant</th>
                  <th className="pb-3 font-bold">Category</th>
                  <th className="pb-3 text-right font-bold">Amount</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-[#1F2937]/50">
                {filteredTransactions.map((tx: any) => (
                  <tr key={tx.transaction_id} className="hover:bg-[#1F2937]/20 transition-colors group">
                    <td className="py-4 text-gray-500 font-mono text-xs">{tx.date}</td>
                    <td className="py-4 font-bold text-gray-300 group-hover:text-white transition-colors">{tx.name}</td>
                    <td className="py-4">
                      <span className={`px-2 py-1 bg-[#030712] border ${tx.category.includes('Income') ? 'border-green-400/50 text-green-400' : 'border-[#1F2937] text-gray-400'} rounded text-[9px] font-bold uppercase tracking-wider`}>
                        {tx.category[0]}
                      </span>
                    </td>
                    <td className={`py-4 text-right font-mono font-bold ${tx.category.includes('Income') ? 'text-green-400' : 'text-[#38BDF8]'}`}>
                      {tx.category.includes('Income') ? '+' : ''}₹{tx.amount.toFixed(2)}
                    </td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
      </section>

      {/* INTELLIGENCE SIDEBAR (30%) */}
      <aside className="w-[30%] bg-[#0B1117] p-10 border-l border-[#1F2937] overflow-y-auto flex flex-col gap-10">
        
        {/* DYNAMIC NET CASHFLOW GAP */}
        <section>
          <p className="text-[#38BDF8] text-[10px] font-bold tracking-[0.2em] mb-4 uppercase">Net Cashflow Gap</p>
          <div className="flex flex-col gap-4">
            <div>
              <p className="text-gray-500 text-[10px] font-bold uppercase tracking-widest mb-1">{timeRange}D Income</p>
              <h2 className="text-4xl font-black text-green-400">₹{dynamicIncome.toLocaleString('en-IN', { maximumFractionDigits: 2 })}</h2>
            </div>
            <div>
              <p className="text-gray-500 text-[10px] font-bold uppercase tracking-widest mb-1">{timeRange}D Outflow</p>
              <h2 className="text-4xl font-black text-white">₹{dynamicOutflow.toLocaleString('en-IN', { maximumFractionDigits: 2 })}</h2>
            </div>
          </div>
        </section>

        {/* PREDICTIVE ADVISOR */}
        <section className="bg-[#030712] p-6 border border-[#1F2937] rounded-lg relative overflow-hidden group hover:border-[#818CF8]/50 transition-colors">
          <div className={`absolute top-0 left-0 w-1 h-full ${activeView === 'forecast' ? 'bg-[#818CF8] shadow-[0_0_15px_#818CF8]' : 'bg-[#38BDF8]'} transition-all`}></div>
          <h3 className="text-[#818CF8] text-[10px] font-bold mb-2 uppercase tracking-[0.15em]">Predictive Advisor</h3>
          <div className="flex items-baseline gap-2">
            <p className="text-white text-5xl font-black tracking-tighter">{dynamicRunway}</p>
            <span className="text-gray-400 text-xs font-bold uppercase tracking-widest">Days Left</span>
          </div>
          <p className="text-gray-500 text-[10px] mt-3 leading-relaxed">
            Calculated using ₹{dailyNoiseRate.toFixed(0)}/day average noise + your exact recurring bill dates.
          </p>
        </section>

        {/* GHOST RAILS */}
        <section className="flex-1">
          <h3 className="text-[#38BDF8] text-[10px] font-bold mb-5 uppercase tracking-[0.15em]">Ghost Rails Detected</h3>
          <div className="space-y-4">
            {data.sidebar.insights.map((sub: any) => (
              <div key={sub.name} className="p-5 bg-[#030712] border border-[#1F2937] rounded-lg hover:border-[#38BDF8]/50 transition-all group cursor-pointer shadow-inner">
                <div className="flex justify-between items-center mb-1">
                  <p className="text-gray-500 text-[10px] font-bold uppercase tracking-tight">Recurring Leak</p>
                  <div className="w-1.5 h-1.5 rounded-full bg-[#38BDF8] group-hover:animate-ping"></div>
                </div>
                <h4 className="text-white text-lg font-bold group-hover:text-[#38BDF8] transition-colors">{sub.name}</h4>
                <div className="flex justify-between items-end mt-2 border-t border-[#1F2937]/50 pt-2">
                  <div>
                    <p className="text-[9px] text-gray-500 uppercase tracking-widest mb-0.5">Per Charge</p>
                    <p className="text-xl font-black text-[#818CF8]">₹{sub.amount}</p>
                  </div>
                  <div className="text-right">
                    <p className="text-[9px] text-red-500/80 font-bold uppercase tracking-widest mb-0.5">90D Total Drain</p>
                    <p className="text-xl font-black text-red-400">₹{sub.total_spent_90d.toFixed(2)}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </section>
      </aside>
    </main>
  );
}
</file>

</files>
